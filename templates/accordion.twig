<div
    class="e-accordion {% if classNames is iterable %}{{ classNames | join(' ') }}{% endif %}"
    style="width: 100%; margin: auto; border: 1px solid #ccc; background-color: #eee; padding: 10px;"
>
    {{ children | join | raw }}
</div>
